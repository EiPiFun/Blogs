# modprobe

sudo vi /etc/modprobe.d/eipifun.conf

```
blacklist nvidia
blacklist nvidia-drm
blacklist nvidia-uvm
blacklist nvidia-modeset
```

# Update initramfs

# systemd

sudo systemctl disable nvidia-persistenced nvidia-hibernate nvidia-resume nvidia-suspend
